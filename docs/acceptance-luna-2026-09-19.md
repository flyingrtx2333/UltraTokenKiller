# Luna 原生链路验收（2026-09-19）

## 结论

Windows Codex 0.138.0 使用现有订阅、`gpt-5.6-luna`，通过隔离 UTK 实例完成输入压缩、工具输出压缩和 MCP 原文找回。回答策略为 lite，命令退出码为 0，未超时，原 Codex 配置校验不变。这是单任务链路通过，不是完整上游对标或发布认证。

## 真实任务证据

- 最后一次尝试使用 6 次模型提交，累计使用本轮授权预算 **16/20**。
- 两条只读命令完成，其中一条显式使用 `utk exec`；不把它宣称为自动钩子覆盖。
- 1 条工具输出压缩统计，3 次 MCP `utk_retrieve` 完成事件，模型返回预期标记。
- 5 条请求记录包含输入压缩结果，其中可能包含稳定重放的历史，不代表 5 份独立原文。
- 提供商 usage 记录数为 0；实际 token 和费用未知。
- 预检使用独立会话，真实报告只统计该次模型任务的会话，不累计旧尝试的压缩记录。
- 脚本在结束后停止隔离服务，未替换现用代理。

本机机器可读证据：`output/codex-luna-20260919/report-016.json`。
预算账本：同目录 `verification-budget.sqlite3`。前 3 次未通过的尝试共消耗 10 次，失败记录保留，没有将它们计为成功。

## 本轮补齐

1. 原样发送过的历史也固定处理结果，切换档位不再改写已有缓存前缀；元数据容量不足时冻结新增有损压缩。
2. Git 工作目录参数、暂存差异及上下文参数；Git 提交正文中的风险、否定、数字和路径保留。
3. `python -m pytest` 识别；diff/search 工具压缩分派；未知补丁格式透传。
4. JSON 少数状态、布尔值、空值和单位代表记录保留。
5. 会话工具统计由守护进程写入，解决 Windows 沙箱写共享 SQLite 的磁盘 I/O 错误。提交接口有本地认证、Origin 校验、严格字段限制和执行标识去重；指标写入失败不改变命令结果。
6. 验收客户端超时后清理整个自有进程树；Windows MCP 和验收脚本明确使用 UTF-8。
7. 网页重新构建并同步随包资源。

## 离线验证

- Windows Python 3.10：完整测试 **86 项通过**。
- WSL Ubuntu：使用 `/tmp/utk-luna-py311` 独立 Python 3.11 环境，完整测试 **86 项通过**，包含 Linux 进程组取消检查。
- 临时 Git 仓库真实执行暂存差异，保留变更行并验证逐字原文恢复。
- 真实 pytest 命令分别执行成功与失败任务，验证压缩、失败详情与退出码。
- 命令各执行一次，同一份输出用于验证压缩；未为统计重放写入命令。
- 前端生产构建通过。测试存在依赖弃用警告，无失败。

WSL 未安装原生 Codex，也未复制 Windows 订阅凭据到 WSL；真实 Luna 客户端验收发生在 Windows。

## 未满足的完整发布门槛

完整 RTK 命令清单实现与上游对照、图像压缩、跨语言语义质量、正常客户端自动会话/钩子绑定、Hermes、真实代码修改任务、回答精简正确性对照、三次重复效果评测，以及三平台干净安装恢复仍未完整验收。当前不发布正式完整对标版本，也不默认接管本机现用代理。

## 显式复现入口

先运行 `scripts/verify_codex_live.py --model gpt-5.6-luna --run-id <同一验收批次> --request-limit <已授权上限> --offline-preflight`。

移除 `--offline-preflight` 才会调用模型。重用同一批次账本；提高命令行上限不能提高已保存的上限。不要通过更换批次绕过授权预算。本次保留的 4 次请求尚未使用。
