# Headroom 对照与补齐 TODO

对照来源：[Headroom 原仓库](https://github.com/chopratejas/headroom)，固定提交 `bc21c9370793f7e4aa94ac4c5d9a67a8d2dd0df9`；本机检出位于 `.tmp/fixed-upstream-14b2391/headroom/`，只是可重建的临时副本，不是交付依赖。复核时从原仓库检出该提交，不以本机 `.tmp` 目录为证据。这是候选实施清单，不表示已实现、已上线、已纳入本版范围或与原版等效。UTK 运行时保持原生实现，不安装或调用 Headroom；开发对照测试可以运行固定原版。每完成一项，更新测试、`full-parity-inventory.json` 的证据和当前状态文档；安全透传不算压缩通过。

当前基线：现有 Headroom 核心清单 24 项，UTK 实现状态为 21 项离线通过、3 项真实客户端通过；原版逐项证据为 9 项样例通过、1 项基准套件通过、14 项未逐项比较。这 24 项不是原版全部功能的分母。实现状态、固定上游行为对照、真实客户端验收必须分别记录。日常 Hermes 曾观测到工具输出节省，但输入节省为 0；不能据此宣称输入压缩已生效。

## 先查当前问题，再确定本版范围

- [x] H00 线上输入节省为 0：先只读核对日常 Hermes 的工具 Hook、实际模型请求结构、有效会话标识、代理可识别的工具结果字段，以及压缩前后大小和跳过原因；区分“Hook 已先压缩”“代理未识别”“压缩器安全跳过”和“统计漏记”。再用未预压缩的长工具结果做离线端到端复现，验证关键事实、失败信息、原文恢复、模型请求中的实际改写和输入节省记录。定位责任层后才开最小修复项；真实 Hermes/模型复验需另行确认预算，失败和重试也计数。不得把工具 Hook 的节省重复算作输入节省，也不得用离线通过冒充线上生效。

H00 验收（2026-09-23）：线上只读记录显示此前工具 Hook 曾节省 122,721 估算 Token，而输入节省仍为 0；一份 Hermes 请求的脱敏结构有有效会话头、8 条工具消息，最长工具消息约 50 KB 且已有 UTK 恢复标记。上线候选数与脱敏跳过原因后，日常 Hermes 的未预压缩日志在下一轮模型请求中实际缩短 508 估算 Token，续聊通过 `utk_retrieve` 找回 300 条原文；多轮重复发送按各次模型请求分别计数，累计输入节省达到 2,540 估算 Token，不与工具 Hook 节省混算。另发现 Hook 将命令写成裸 `utk`，在 Hermes 的 PATH 中退出 127；已改为稳定绝对路径，线上同类命令恢复为退出 0。该次命令结果没有可安全缩短的部分，记录为跳过。新增 20 次额度中使用 15 次，余 5 次；一次失败场景出现 HTTP 200 的上游流式错误，脱敏错误分类的本地修复已通过回归，但尚未发布，不将其称为线上已修复。上述 Token 节省是 UTK 估算，不是供应商账单实省。

H01–H17 已按用户要求全部纳入交付清单；逐项验证后才勾选。H16 的盘点仍须明确各额外机制是否纳入 UTK 正式能力，H17 只对选入的机制实施，未选机制明确列为本版范围外，不得计入等效声明。

2026-09-23 追加真实模型调用预算 15 次；此前剩余 5 次，当前剩余上限 20 次。此项只增加额度，不表示已经完成后续真实客户端验收。

截至 2026-09-24：线上预算已用 12/25 次，剩余 13 次。本次 Hermes 烟测计 2 次（主请求 1 次、标题生成 1 次）；请求成功但没有工具调用，不能据此证明压缩生效。

## A. 行为缺口：先做安全、可测的核心链路

- [x] H01 混合内容分段路由：对一个工具结果中交错的 JSON、代码、日志、搜索、HTML、表格和普通文本分别识别、压缩、重组；保留顺序、边界、引用和失败信息。参照原版 `headroom/transforms/content_router.py`；落点 `src/ultratokenkiller/compression.py`、`proxy.py`。验收：混合样例与原版固定输出做事实级对照，未知片段原样通过。

  验收通过（2026-09-23，本地）：固定 Headroom 相邻内容样例的事实断言、表格/HTML/普通文本复杂交错、未知内容透传、失败字段保留、整段原文恢复，以及模型代理转发路径均通过；代码围栏不破坏结构。测试：`tests/test_recovery_pipeline.py`、`tests/test_protocols_v2.py`。本地自动化验证，不代表真实 Hermes 调用。
- [ ] H02 普通长文本压缩策略：研究原版 `headroom/transforms/kompress_compressor.py` 的 `kompress-v2-base` 路径、模型缓存、超时和回退；决定 UTK 原生模型或明确的非等效替代，不以当前 MiniLM/确定性筛选冒称复刻。落点 `text_compression.py`、`compression.py`。验收：中英文长文的事实保留、长度、延迟、不可用回退成组对照；下载/许可/资源成本单独审批。
  初查：固定原版会懒加载并缓存 Hugging Face `kompress-v2-base`，有 20 秒请求期限及模型不可用时旁路；UTK 采用 Apache-2.0 的 `all-MiniLM-L6-v2` 抽取评分和独立的中文确定性筛选，明确不等效。当前运行环境没有 UTK 的约 90.4 MB ONNX 模型文件，因此英文真实编码器的压缩率与延迟还不能验收；缺模型时已验证原文安全透传。依照此项的资源成本要求，本轮未下载模型，H02 不勾选。
- [x] H03 缓存前缀保护与冷前缀再压缩：明确不可变前缀边界、TTL、思维块和缓存命中保护；只在允许的冷前缀上处理。参照 `cache_aligner.py`、`cold_prefix.py` 和 `pipeline.py`。验收：缓存前缀内容不变、续聊稳定、不同供应商格式测试通过。
  验收（2026-09-25，本地）：代理按会话、协议和模型跟踪收到与转发的历史；首次接入冻结已有历史，续聊只压缩可确认的新增消息，重复请求复用先前转发结果。读取生命周期替换、普通工具结果及图片共享冻结边界，响应风格不改写已接入会话的系统前缀；思维块、签名和缓存标记保留。状态限定为 128 个会话、总计 32 MiB、每会话 4 MiB、空闲 2 小时；超限或失配时原样转发已有历史。明确 `5m`/`1h` TTL 且距上次请求超过 TTL 加 60 秒、历史尚未由 UTK 改写时，才允许冷重压并生成可恢复原文的新句柄；未知 TTL、恢复状态不可信时保持冻结。测试覆盖 OpenAI Chat Completions、Anthropic Messages、Responses API、WebSocket、并发、过期、图片和风格变化。全量本地测试 439 passed、3 skipped。供应商真实缓存命中尚未验证；代理重启后会按首次接入冻结客户端历史，若重启前代理曾改写前缀，无法保证其仍与供应商此前缓存内容相同；本项不宣称上游内部等效。
- [x] H04 跨轮重复内容消除：仅在同一真实会话中识别重复段，生成可恢复引用；无会话、过期、重启后索引缺失时保留原文。参照 `cross_turn_dedup.py`。验收：多轮重复节省大于零，跨会话不串数据，关键行号/路径不丢。
  验收（2026-09-23，本地）：UTK 除完全重复结果的短引用外，还会在同一会话最近 8 条原始结果（累计不超过 2,000,000 字符）中查找精确相同的连续行段；只处理至少 4 行且不含错误/警告、路径、行号和常见 ID 的片段，单段上限 12,000 字符，不做模糊匹配。引用同时指向先前结果句柄；当前结果也有独立恢复句柄，分别取回前次片段所在全文及本次完整原文。离线用例确认节省大于零、失败信息和 `src/module.py:42` 保留、跨会话隔离、过期/重启/无会话不沿用索引。已有代理多轮用例继续验证同一结果引用。实现差异：这是保守的行级精确匹配，不复刻原版的数据结构或启发式；未单独做真实 Hermes 的部分重复段调用。本地定向测试 35 passed；当前全量回归 403 passed、3 skipped。
- [x] H05 文件读取生命周期：区分首次读取、重复读取、覆盖读取和后续变更，避免把旧文件片段错误当新内容。参照 `read_lifecycle.py`。验收：读—改—重读、部分范围读取、并发文件路径用例事实一致。
  验收（2026-09-23，本地）：代理按 OpenAI、Anthropic 与 Responses 工具调用中的路径和操作顺序，分别识别读取、编辑/写入；后续编辑/写入会将旧读取替换为要求重新读取的提示，后续读取只有完整覆盖旧范围才会替代旧结果，部分范围互不覆盖。未变更的读取结果不进入通用压缩。显式 `cache_control` 标记以前的前缀不改写；不足 512 字节的结果旁路。替换结果绑定当前会话，`utk_retrieve` 可恢复被替换的旧输出。测试覆盖读—改—重读、Write、全量/局部范围覆盖、不同文件并发、OpenAI/Anthropic/Responses 结构、小结果旁路、缓存前缀与恢复。实现差异：UTK 仅从显式缓存标记推断冻结前缀，未实现上游的 CCR 持久存储，也未单独做真实 Hermes 生命周期调用；不宣称上游等效。全量测试 403 passed、3 skipped。
- [x] H06 工具结果拦截与安全旁路：核对原版 `pipeline.py` 的工具结果拦截顺序、超时、熔断、异常透传、已压缩标记与压缩收益门槛。验收：慢处理、异常、二次压缩、失败退出码均不破坏原始调用；记录跳过原因。

  验收（2026-09-23，本地）：UTK 在输入代理设置 10 秒压缩上限、连续 3 次失败后熔断 30 秒；超时、异常、熔断均向上游原样传递请求，工具状态包中的退出码和 stderr 不变，跳过原因不含异常正文。既有已压缩内容不重复计数；对照原版 `headroom/transforms/pipeline.py` 的安全旁路与熔断机制，UTK 保持独立实现。相关测试位于 `tests/test_structured_tool_outputs.py`；全量回归 369 passed、3 skipped。本项是本地验收，尚未部署到日常服务器。
- [ ] H07 模型级 Token 计数与净收益决策：对照 `headroom/tokenizers/registry.py`、`transforms/compression_policy.py`；分别记录改写前后的估算 Token、供应商返回的实际用量和按明确价格表估算的金额。实际用量没有反事实对照时，不称为账单实省；缓存读写、模型价格未知时不虚构净收益。价格和缓存条件已知时，只在预估净收益为正且安全时按该策略改写；未知时只能依据安全性与估算 Token 缩减决策，并标注金额无法判断。验收：模型切换、未知模型、缓存价差和多次读取的决策测试，三种口径在事件和页面上不混用。
  进行中（2026-09-23，本地）：已知模型使用对应 tiktoken 编码；未知模型使用带标签的近似计数。新增可选 `model_pricing` 精确模型价格表，要求填写输入/输出单价、币种、价格来源、核对日期和缓存规则。图表与活动分别列出供应商实际用量×单价的费用估算，以及仅在确认无缓存时给出的输入费差额；缓存价格或缓存命中情况不明确时不猜金额。未配置价格时旧数据和 Token 统计保持兼容，公网活动不返回价格来源字符串。新增模型切换、未知模型、缓存单价、缓存用量缺失、多次请求聚合和公开脱敏测试（定向测试 23 passed）。本地构建通过。仍不勾选：线上 custom 提供商的单价与缓存策略未录入，未用账单核对，也未做真实客户端页面验收；未知价格时的压缩仍只按安全性和 Token 缩减判断，不声称完成净收益决策。
- [x] H08 恢复语义闭环：对照原版会话存储、恢复工具注入与失效处理；UTK 的 `recovery.py` 保持运行环境绑定会话，不允许模型指定别人的会话。验收：压缩→取回原文、多轮/并发/过期/重启、恢复失败原因与隐私隔离。

  验收（2026-09-23）：固定原版 `headroom/cache/compression_store.py`、`ccr/mcp_server.py` 使用可取回标记与恢复工具；UTK 用内存会话库和 `utk_retrieve`，不宣称存储实现等同。Hermes 多轮真实测试已取回 300 条原文；本地测试覆盖并发会话隔离、错会话拒绝、过期、重启后不可恢复、模型提供的会话覆盖被忽略及 Broker 鉴权。UTK 的失败提示合并过期/重启/错会话原因，避免泄露句柄是否属于其他会话；服务重启会清空内存原文，这是明确限制。新增并发/重启测试位于 `tests/test_recovery_pipeline.py`。
- [x] H09 图像决策：对照原版图像压缩触发、格式/尺寸、质量与回退，不只证明请求可以携带图片。验收：图片可读性、尺寸收益、损坏/不支持格式原样通过；多模态请求结构不变。

  验收（2026-09-23，本地）：对照固定原版 `headroom/transforms/content_router.py::optimize_images_in_messages`；原版可用模型路由，UTK 使用确定性规则，不能宣称决策等效。UTK 对文字密集和精细识别任务原样通过；对大幅照片压到最长边 1600 像素且保留可取回原图；像素差异、损坏/不支持格式、小图回退及 OpenAI/Anthropic 多模态块顺序由 `tests/test_image_compression.py` 验证。本项未做真实模型视觉质量对照，不将像素收益称为账单 Token 节省。

## B. 已有入口：补固定原版逐项证据，不预设要重写

- [ ] H10 对照 `input.long_text_zh`、`input.image`：固定样本、关键事实、压缩率和回退原因；若差异超出约定，再拆实现修复项。
  部分验收（2026-09-23，本地）：固定中文样本 360 字，UTK 输出 196 字（字符比 54.4%），固定提交 Headroom 输出 71 字（19.7%）；两条含路径/数字/限制条件的关键句双方均保留。上游 `_core` 由固定提交本地离线编译，相关 Rust 测试 13 项通过；样本及二进制摘要见 `docs/evidence/headroom-h10-20260923.json`。
  图像样本为 2600×2200 合成照片：UTK 输出 1600×1354，字符比 5.47%；Headroom 的 OpenAI tile 优化输出 605×512，字符比 0.40%，随后因本机没有学习路由模型而走 `preserve`。测试时禁止网络下载。像素差异只针对合成噪声图，不代表真实图片可读性验收；缺少模型路由的实测，故 H10 仍未完成。
- [x] H11 对照 `recovery.memory`、`recovery.mcp`、`cache.stable_prefix`：会话隔离、原文恢复、缓存边界；现有真实客户端通过不替代原版对照。
  验收（2026-09-23，Windows）：固定提交本地编译的 `headroom._core` 后，上游压缩存储、MCP 恢复、缓存前缀稳定性/覆盖层/跟踪器测试合计 184 passed；无失败、无网络或模型 API 调用。UTK 侧完整回归 420 passed、3 skipped。源码提交、二进制摘要和测试文件见 `docs/evidence/headroom-h11-20260923.json`。上游与 UTK 的存储实现不同，只记录行为样例通过，不宣称内部实现相同。
- [x] H12 对照 `protocol.responses_http_sse`、`protocol.chat_completions`、`protocol.anthropic_messages`、`protocol.responses_websocket`：非流式/流式、工具调用、取消、错误、重连；安全透传单列。
  验收（2026-09-23，Windows/离线）：固定 Headroom 提交 `bc21c9370793f7e4aa94ac4c5d9a67a8d2dd0df9` 的 16 个协议相关测试文件共 237 项全部通过、无跳过；UTK `tests/test_protocols_v2.py` 19 项通过，全量回归 425 passed、3 skipped。覆盖 Chat Completions SSE 原样转发与用量记录、Anthropic/Responses 流式解析与安全旁路、工具结果压缩及原文恢复、WebSocket 取消/错误、握手阶段最多 3 次短退避重试，以及连接中断后记录未完成请求并允许客户端显式重连；已提交模型请求不自动重放，失败/取消不记压缩节省，错误正文不写入活动记录。上游扩展模块使用同提交离线编译产物，摘要和测试文件见 `docs/evidence/headroom-h12-20260923.json`。这是协议级离线行为验收，不代表 Codex/Hermes 真实客户端或线上模型验收；这些另由 H13 验证。
- [ ] H13 对照 `client.codex_subscription`、`client.codex_api_key`：配置保持、鉴权、实际请求与恢复；真实客户端测试需要单独预算和环境，不能由离线测试推断。
- [x] H14 对照 `dashboard.metrics`：请求、压缩类别、Token 估算/供应商实际用量/金额估算、时间、失败、缓存与跳过理由的口径；工具 Hook 与输入代理节省分开，页面数字能追到原始记录，不包含正文/凭据/会话标识。验收（2026-09-23，本地）：后端统计全量回归通过；图表包含请求/失败、供应商输入/输出/缓存、费用和节省趋势；活动详情只展示安全类别、错误类别、回退原因与计数。桌面/窄屏通过合成数据渲染，私密路径未显示。证据：`docs/evidence/headroom-h14-20260923.json`。不代表真实客户端或线上验收。

线上补验（2026-09-24）：新版看板已发布，公网桌面（1440×1100）和手机（390×844）页面均能加载图表，页面本身没有横向溢出；手机趋势图在卡片内横向滑动。结果图会合并模型和工具事件；线上 24 小时记录为模型请求 26 成功、1 失败，工具事件 2 成功，因此显示 28 成功、1 失败，与记录一致。已将标题改为“事件结果（模型与工具）”避免误读；最终 HTML/JS 已在线，JS 返回 200 且包含新标题，但刷新浏览器确认新标题时超时。Hermes 请求成功并被统计，但本次烟测未触发工具调用，输入压缩节省仍未得到真实验证。费用图暂无可估价数据，因为尚未配置对应模型价格。证据：`docs/evidence/headroom-h14-live-20260924.json`。之前截图是在标题修改前截取，最终版本截图未保存。
- [ ] H15 对照 `install.windows`、`install.macos_linux`：全新安装、升级、重复接入、停用恢复、带空格路径、权限与失败回滚；不修改用户原有模型/凭据。

## C. 原版额外机制：先判定产品范围，再决定是否实现

- [ ] H16 盘点原版 `headroom/proxy/` 中当前 24 项未单列的能力，逐项标注“核心/可选/实验/UTK 不适用”，给出源码入口、现有 UTK 对应、最小验收；重点核对工具 schema 压缩、输出 shaping、语义缓存、系统消息压缩、后台压缩、Gemini/Bedrock、批处理、遥测与路由。不得把这些自动计入“已复刻”。
- [ ] H17 对 H16 被选为正式范围的能力，各开独立实现项和回归用例；未获选择的能力明确写为“不在本版范围”，不要笼统说“完整复刻 Headroom”。

## 执行和完成判据

建议顺序：H00 → H06 → H01/H07 → 其余项目。相互独立的对照项可单独完成。每项 PR 或提交写明原版函数、UTK 函数、固定输入/输出、差异、测试平台和回退方式。真实模型/客户端调用受已确认预算约束，失败和重试也计数；预算用尽不能用离线结果冒充真实验收。只有清单中各项满足自身判据，才可全部勾选；不能把 24 项核心清单当作 Headroom 全量复刻证明。
