# Codex Terra 验收记录（2026-09-19）

本记录是局部验收证据，不是完整复现或正式发布认证。

## 真实客户端验收

- 客户端：Codex 0.138.0；最终验收模型：`gpt-5.6-terra`。
- 认证：沿用现有订阅；未切换收费 API。
- 隔离运行 UTK，未修改原 Codex 配置；未替换用户正在使用的代理。
- 最后一次任务完成 MCP `utk_retrieve` 调用，返回预期标记，退出码为 0，未超时。
- 最后一次任务消耗 2 次提交；累计预算已达 **10/10**，不得继续提交模型请求。
- 本轮累计包含切换至 Terra 前的 2 次提交；不能把全部 10 次计为 Terra 验收。
- 提供商 usage 记录为 0：实际 token 用量未知，不能据此计算节省。
- 最后一次是原文找回专项任务，工具输出压缩记录为 0：不证明执行钩子或三层联动全部通过。

本地机器可读报告：`output/codex-live-verification/report.json`。
预算账本：同目录 `verification-budget.sqlite3`。
报告和账本属于本地验收产物，不包含订阅凭据。

## 离线回归

当前工作区执行 `python -m pytest -q`：**61 passed**。
存在 Starlette 和 pytest-asyncio 弃用提示，未出现测试失败。
这些测试通过不等于上游能力清单已覆盖或跨平台已验收。

## 仍未通过的发布条件

- 上游命令发现清单尚未全部人工核对、实现和对照验收。
- 输入压缩与回答精简尚无完整上游效果对照；图像能力未完成。
- 正常客户端会话自动绑定、执行钩子覆盖与全链路关联尚未完整验收。
- Hermes 未安装，CLI/gateway 真实任务未验收。
- macOS/Linux 和干净 Windows 环境安装恢复验收未完成。
- 压缩率、真实任务成功率及三次重复对照等发布门槛未满足。

后续继续离线实现与测试；真实模型预算耗尽后不自动重置，也不改用其他收费路径。
