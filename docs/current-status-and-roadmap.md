# UltraTokenKiller 当前状态与开发路线图

状态刷新日期：2026-09-22。本文档描述当前证据绑定结果与后续开发顺序；历史日期报告继续保留其运行当时的环境和结论。

## 当前状态

<!-- BEGIN GENERATED CURRENT STATUS -->
高层产品能力共 **36 项**：**33 项**证据有效并离线通过，**3 项**已实现但当前证据待重新验证，**0 项**拥有当前源码绑定的实机通过证据。

完整三层核心分母共 **256 项**：固定上游完整通过 1 项、部分通过 40 项、离线通过 42 项、仅完成契约映射 30 项、资产未就绪 2 项、未实现 141 项。

按上游拆分的证据绑定状态：

- Headroom：1 项固定上游完整通过、21 项离线通过、2 项资产未就绪。
- RTK：40 项固定上游部分通过、30 项仅完成契约映射、141 项未实现。
- Caveman：0 项固定上游完整通过、21 项离线通过。
<!-- END GENERATED CURRENT STATUS -->

最近完成的验证批次：

- Windows 全量测试：`334 passed, 3 skipped`。
- macOS arm64 全量测试：`336 passed, 1 skipped`。
- Windows 与 macOS 固定 RTK：66 个捕获输出用例。
- `gh`、`glab`、`gt` hosting 批次：21 个用例，上游事实失败 0、UTK 事实失败 0、透传失败 0、模型调用 0。

这些结果只证明对应提交、平台和用例已通过，不代表 Linux、干净安装、真实 Hermes 客户端、全部 256 项能力或正式发布已经验收。

## 后续开发计划

### 1. 修复证据账本（已完成）

2026-09-22 已在完整测试通过后刷新当前源码与测试指纹，9 个仅因指纹过期而降级的能力恢复为离线通过。运行时报告、机器账本和三个当前状态文档已一致；Caveman 成对质量与 Hermes CLI/gateway 仍按真实边界保留为待验证。

### 2. 完成现有 RTK 契约（下一阶段）

先补齐已映射的测试、构建、诊断和包管理等契约。每项覆盖成功、失败、未知输出及适用平台；安全透传不计为压缩实现。完成条件是 70 个已映射变体都有明确实现和证据结论。

### 3. 实现剩余 141 个 RTK 变体

按通用 52、JavaScript/TypeScript 40、云与基础设施 18、Rust 12、.NET 5、JVM 5、Git 4、Go 4、数据工具 1 的顺序分批推进。每批同时提交契约、回归测试、固定上游对照和清单更新，不能只增加命令识别规则。

### 4. 补齐 Headroom

完成剩余 14 项逐项固定上游比较，并安装、验证两个缺失的本地资产。完成条件是 24 项均有逐项结论且不再存在 `asset_not_ready`。

### 5. 执行 Caveman 质量评测

获得新的明确授权后，执行五类场景、各档位、三次重复的 180 次模型请求。授权前保持 `authorization_required`，不以离线策略测试替代真实回答质量结论。

### 6. 完成客户端与平台矩阵

完成 Codex、Hermes CLI/gateway 及 Windows、macOS、Linux 的真实任务、干净安装、升级和恢复验证。所有实机结论必须绑定当前源码指纹和证据文件。

### 7. 发布验收

前述门槛通过后再刷新 Dashboard 截图、浏览器交互证据和发布产物。最终门槛包括全量测试、生成内容无漂移、三平台安装恢复、真实客户端任务及证据泄密检查。

## 维护检查

```sh
python scripts/generate_current_status.py --check
python -m pytest tests/test_current_status_docs.py tests/test_capability_evidence.py tests/test_full_parity_inventory.py
```

状态生成只维护标记区块；本页的开发顺序、完成条件和授权边界由维护者审阅，不由统计脚本改写。
