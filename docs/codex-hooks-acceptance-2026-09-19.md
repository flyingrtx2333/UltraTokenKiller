# Codex 自动钩子新增预算验收

2026-09-19，Windows Codex 0.138.0，现有订阅，`gpt-5.6-luna`，回答策略 lite。用户新增授权最多 10 次模型提交，独立账本 `codex-luna-hooks-20260919`，实际累计 8 次，剩余 2 次。旧账本未重置。未切换收费 API，未替换本机代理。

## 实测结果

| 项目 | 第一次（report-004） | 第二次（report-008） |
|---|---|---|
| 本次模型提交 | 4 | 4 |
| 自动钩子关联的工具压缩 | 1 | 1 |
| MCP 原文查询 | 完成 | 完成 |
| 编码任务 | 仅目标源码改变，测试文件未改，7 项测试通过 | 修改命令失败，源码未改，独立测试未通过 |
| 完成标记 | 有 | 无 |
| Codex 进程退出码 | 0 | 0 |
| 全局配置字节未变 | 否 | 否 |
| 整体验收 | 未通过 | 未通过 |

任务初始测试为 3 项失败、4 项通过。第一次证明了自动钩子、恢复、修复、独立复测可以连通，但配置保护门槛未通过，不能据此宣布自动接入验收成功。第二次进一步说明客户端退出码 0 不代表工具或任务成功。

两次均未返回可记录的实际 usage，应显示未知。输入层没有新增压缩记录；该任务的工具输出已经过工具层压缩，不将此计为输入层压缩缺陷，也不宣称双层收益。

## 配置诊断与边界

第一次未记录分阶段配置差异，无法追溯具体变化。随后增加仅含布尔值和允许列表内顶层配置名称的诊断，不保存配置内容、路径、认证值或模型正文。

第二次确认：离线预检前后配置字节和解析结果均相同；客户端运行期间，仅 `projects` 顶层段发生语义变化。其他顶层段没有语义变化。具体写入者、项目条目和触发机制尚未确定，不将其解释为已解决，也不自动覆盖用户配置。

第二次修改命令失败的具体原因尚未确定。默认不保存模型工具正文，因此持久报告仅保留失败状态与退出码，不能从报告反推具体命令错误。

## 后续门槛

先离线定位项目配置写入与修改命令失败，补充针对性回归证据，再进行完整复测。剩余 2 次低于现有完整编码场景的最低 4 次检查门槛，本批停止模型提交，保留剩余额度。

局部回归 `tests/test_acceptance_evidence.py` 已通过，覆盖格式变化与配置语义变化的区分、诊断不泄漏配置值。此次未重跑全量或 WSL 测试；此前 97 项结果不作为本次新增代码的全量验证。

该结果不覆盖 Hermes、完整 RTK 命令能力、三平台安装或固定上游性能对照。自动接入继续标记为开发预览。
